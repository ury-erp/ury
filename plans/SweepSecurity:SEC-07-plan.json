{
  "finding_id": "SEC-07",
  "objective": "Decompose security finding SEC-07 into structured, trackable implementation subtasks prior to applying code fixes, defining authorization and same-branch checks for merge_bills in ury_pos/api.py.",
  "scope": [
    "ury/ury_pos/api.py"
  ],
  "priority": "High",
  "risk_metrics": {
    "severity": "Medium",
    "impact": "High",
    "likelihood": "Medium"
  },
  "acceptance_criteria": [
    "Enforce frappe.has_permission(\"POS Invoice\", \"write\", doc=primary_doc, throw=True) and doc=secondary_doc to verify write access before merging.",
    "Enforce branch matching (primary_doc.branch == secondary_doc.branch) to prevent cross-branch merging."
  ],
  "tasks": [
    {
      "id": "SEC-07-1",
      "description": "Add frappe.has_permission checks for both primary and secondary POS Invoices in merge_bills.",
      "status": "completed"
    },
    {
      "id": "SEC-07-2",
      "description": "Add a branch matching check in merge_bills.",
      "status": "completed"
    }
  ]
}