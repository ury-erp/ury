# URY Features Mapping

This directory maintains the mapping of URY app features to their corresponding codebase structure, Doctypes, frontend components, and API endpoints.

When creating full documentation or adding new features, document the mapping here so agents can easily navigate the relationship between user-facing features and the underlying code.
